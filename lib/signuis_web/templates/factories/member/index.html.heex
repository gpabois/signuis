<h1>Listing Factories members</h1>

<table>
  <thead>
    <tr>
      <th>Roles</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for member <- @factories_members do %>
    <tr>
      <td><%= member.roles %></td>

      <td>
        <span><%= link "Show", to: Routes.factories_member_path(@conn, :show, member) %></span>
        <span><%= link "Edit", to: Routes.factories_member_path(@conn, :edit, member) %></span>
        <span><%= link "Delete", to: Routes.factories_member_path(@conn, :delete, member), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Member", to: Routes.factories_member_path(@conn, :new) %></span>
